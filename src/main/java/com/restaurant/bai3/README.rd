Trong Cách A số 5 nằm trong URI Path và là một phần của tài nguyên cần truy cập.
Trong Cách B số 5 nằm trong Query String phía sau dấu ?.
Theo RESTful, khi cần truy cập một đối tượng cụ thể theo ID, nên dùng PathVariable vì URL rõ ràng và mang tính định danh tài nguyên.
Vì vậy, em chọn Cách A